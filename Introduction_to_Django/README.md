# Introduction to Django Development Environment Setup

## Objective
The objective of this task is to gain familiarity with Django by setting up a Django development environment and creating a basic Django project named **LibraryProject**.  
This setup introduces the basic workflow of Django projects — from installation to running the development server and exploring the project structure.

---

## Task Description
I followed the required steps to install Django, create a new project, and explore the default project files.

The process included:
1. Installing Django via `pip`.
2. Creating a new Django project named **LibraryProject**.
3. Running the Django development server.
4. Exploring the project structure and key configuration files.
5. Creating this `README.md` file inside the `LibraryProject` directory.

---

## Step-by-Step Instructions

### 1. Ensure Python is Installed
First, I confirmed that Python was installed by running:
```bash
python --version
